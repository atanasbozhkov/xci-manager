import { HactoolRunner } from '../../src/hactool-runner/hactool-runner';

describe('GIVEN a HactoolRunner', () => {
    it('SHOULD be defined', () => {
        expect(HactoolRunner).toBeDefined();
    });
});
